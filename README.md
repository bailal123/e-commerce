# 🛒 متجر - Multi-Vendor E-Commerce Store

متجر إلكتروني متعدد الأقسام والتجار مبني باستخدام React و Tailwind CSS

## ✨ المميزات

### 🎨 التصميم
- **تصميم عصري**: واجهة مستخدم حديثة وأنيقة
- **الوضع الداكن**: دعم كامل للوضع الداكن
- **RTL Support**: دعم كامل للغة العربية
- **Responsive**: متجاوب مع جميع أحجام الشاشات
- **Animations**: انتقالات وحركات سلسة

### 🛠️ التقنيات
- **React 18**: مع Functional Components و Hooks
- **Vite**: للبناء السريع مع Code Splitting
- **Tailwind CSS**: للتنسيق مع تخصيصات متقدمة
- **React Router**: للتنقل بين الصفحات
- **react-helmet-async**: لتحسين SEO

### 📦 البنية (Atomic Design)
```
src/
├── components/
│   ├── atoms/       # مكونات أساسية (Button, Input, Badge...)
│   ├── molecules/   # مكونات مركبة (ProductCard, SearchBar...)
│   ├── organisms/   # مكونات معقدة (Header, Footer, ProductGrid...)
│   └── templates/   # قوالب الصفحات (MainLayout)
├── pages/           # صفحات التطبيق
├── context/         # React Context (Cart, Theme)
├── hooks/           # Custom Hooks
├── utils/           # دوال مساعدة
├── data/            # بيانات تجريبية
└── styles/          # أنماط CSS عامة
```

## 🚀 البدء السريع

### المتطلبات
- Node.js 18+
- npm أو yarn أو pnpm

### التثبيت

```bash
# تثبيت التبعيات
npm install

# تشغيل في وضع التطوير
npm run dev

# بناء للإنتاج
npm run build

# معاينة البناء
npm run preview
```

## 📄 الصفحات

| الصفحة | المسار | الوصف |
|--------|--------|-------|
| الرئيسية | `/` | صفحة البداية مع الأقسام والمنتجات المميزة |
| القسم | `/category/:slug` | عرض منتجات قسم معين مع الفلاتر |
| المتجر | `/vendor/:slug` | صفحة متجر التاجر |
| المنتج | `/product/:slug` | تفاصيل المنتج |
| السلة | `/cart` | سلة التسوق |
| الدفع | `/checkout` | إتمام الطلب |

## 🎨 الألوان

```javascript
colors: {
  primary: 'indigo',    // اللون الرئيسي
  accent: 'orange',     // لون التمييز
  success: 'emerald',   // النجاح
  warning: 'amber',     // تحذير
  error: 'red',         // خطأ
}
```

## 🧩 المكونات الرئيسية

### Atoms (الذرات)
- `Button` - أزرار متعددة الأشكال
- `Input` - حقول إدخال مع دعم الأيقونات
- `Badge` - شارات الحالة
- `Rating` - تقييم بالنجوم
- `Image` - صور مع Lazy Loading
- `Skeleton` - هياكل التحميل
- `Spinner` - مؤشر التحميل
- `Avatar` - صورة المستخدم
- `Logo` - شعار المتجر

### Molecules (الجزيئات)
- `ProductCard` - بطاقة المنتج
- `CategoryCard` - بطاقة القسم
- `VendorCard` - بطاقة التاجر
- `SearchBar` - شريط البحث
- `CartItem` - عنصر السلة
- `FilterSidebar` - فلاتر البحث
- `ImageGallery` - معرض الصور
- `ProductInfo` - معلومات المنتج
- `ReviewCard` - بطاقة التقييم

### Organisms (الكائنات)
- `Header` - رأس الصفحة مع القائمة
- `Footer` - تذييل الصفحة
- `HeroSection` - البانر الرئيسي
- `ProductGrid` - شبكة المنتجات
- `CategoryGrid` - شبكة الأقسام
- `VendorShowcase` - عرض التجار
- `Testimonials` - آراء العملاء
- `TrustBadges` - شارات الثقة
- `PromoBanner` - بانر العروض

## 🔧 التخصيص

### تغيير الألوان
عدّل ملف `tailwind.config.js`:

```javascript
theme: {
  extend: {
    colors: {
      primary: colors.blue, // غيّر إلى اللون المطلوب
      accent: colors.amber,
    }
  }
}
```

### إضافة خطوط
أضف الخط في `tailwind.config.js` و `index.html`

### تغيير البيانات
عدّل ملف `src/data/mockData.js`

## 📱 الاستجابة

| نقطة التوقف | الحجم |
|-------------|-------|
| `sm` | 640px |
| `md` | 768px |
| `lg` | 1024px |
| `xl` | 1280px |
| `2xl` | 1536px |

## ⚡ الأداء

- **Code Splitting**: تقسيم الكود تلقائياً
- **Lazy Loading**: تحميل كسول للصور والصفحات
- **Tree Shaking**: إزالة الكود غير المستخدم
- **Minification**: ضغط الملفات للإنتاج

## 🔒 الأمان

- حماية XSS مدمجة في React
- Sanitization للمدخلات
- HTTPS جاهز

## 📝 الرخصة

MIT License

---

صُنع بـ ❤️ باستخدام React و Tailwind CSS
